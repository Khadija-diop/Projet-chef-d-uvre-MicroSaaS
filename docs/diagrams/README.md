# Diagrammes UML - Etape B

Ce dossier contient les sources textuelles des diagrammes UML de l'etape B.

Fichiers presents :

- `use-case.puml` : diagramme de cas d'utilisation
- `deployment.puml` : diagramme de deploiement logique

Exports attendus pour le dossier de conception :

- `use-case.png`
- `deployment.png`

Si tu disposes d'un plugin PlantUML dans ton IDE ou d'un rendu PlantUML en ligne, ouvre les fichiers `.puml` puis exporte-les en `PNG` dans ce meme dossier.
